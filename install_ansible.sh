#!/bin/bash

# if we were being sophisticated we could automatically use the correct package manager based on distribution,
# but this script isn't the focus.

# Ubuntu:
sudo apt install ansible
# Fedora:
# dnf install ansible
# CentOS:
# sudo yum install epel-release && sudo yum install ansible